import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#1ea792',
    },
    secondary: {
      main: '#ff9202',
    },
    common: {
      black: '#000000',
      white: '#ffffff',
    },
  },
});

export default theme;
